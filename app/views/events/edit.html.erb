<% content_for :title, "Editar evento - Cerimonial.app" %>

<div class="max-w-4xl mx-auto">
  <%= breadcrumbs ["Eventos", events_path], [event_crumb_label(@event), @event], ["Editar", nil] %>
  <div class="mb-6">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-900">Editar evento</h1>
    <p class="text-gray-500 mt-1">Atualize as informações do evento</p>
  </div>

  <%= render "form" %>
</div>
